# rpi-rgb

## Installation
- `sudo pip install -r requirements.txt`
- `yarn install`
- `gulp`

## Run application
- `export FLASK_APP=app.py`
- `export FLASK_DEBUG=1` (optional)
- `python -m flask run`
